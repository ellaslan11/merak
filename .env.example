# Hugging Face — real BLIP image captions (free token at huggingface.co/settings/tokens)
HF_TOKEN=

# Optional: force cache-only for rehearsals (no API calls)
# MERAK_VISION_MODE=cache

# Optional: BLIP model override
# HF_VISION_MODEL=Salesforce/blip-image-captioning-base

# Pexels — live camera roll photos (free key at pexels.com/api)
PEXELS_API_KEY=

# OpenAI — optional weekly letter generation (/api/generate-letter)
OPENAI_API_KEY=

# Optional model override (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
